# راهنمای تست سریع 🚀

## قبل از شروع:
✅ Backend در حال اجرا: http://127.0.0.1:8000/
✅ Frontend در حال اجرا: http://localhost:5173/

---

## تست 1: ایجاد جلسه (2 دقیقه)

### مراحل:
1. با ادمین یا اسکرام مستر login کنید
   - Username: `admin` / Password: `admin123`
   - یا Username: `scrum` / Password: `scrum123`

2. به صفحه "جلسات" بروید

3. روی دکمه "جلسه جدید" کلیک کنید

4. فرم را پر کنید:
   ```
   عنوان: جلسه تست
   توضیحات: این یک جلسه تست است
   تاریخ و ساعت: یک تاریخ آینده انتخاب کنید
   مدت زمان: 60 (پیش‌فرض)
   مکان: اتاق جلسات
   شرکت‌کنندگان: حداقل یک نفر انتخاب کنید
   ```

5. روی "ایجاد جلسه" کلیک کنید

### نتیجه مورد انتظار:
✅ جلسه ایجاد شود
✅ در لیست جلسات نمایش داده شود
✅ نوتیفیکیشن برای شرکت‌کنندگان ارسال شود

### اگر خطا گرفتید:
- F12 را بزنید و Console را باز کنید
- خطا را یادداشت کنید
- Terminal backend را بررسی کنید

---

## تست 2: جابجایی تسک (1 دقیقه)

### با کاربر عادی:
1. Login کنید:
   - Username: `ali` / Password: `ali123`
   - یا Username: `reza` / Password: `reza123`

2. به صفحه "تسک‌ها" بروید

3. یک تسک خودتان را بگیرید و به ستون دیگری بکشید

### نتیجه مورد انتظار:
✅ تسک جابجا شود
✅ وضعیت تسک تغییر کند

4. حالا سعی کنید تسک شخص دیگری را جابجا کنید

### نتیجه مورد انتظار:
❌ پیام خطا: "شما فقط می‌توانید تسک‌های خودتان را تغییر دهید"

### با ادمین:
1. Login کنید: Username: `admin` / Password: `admin123`

2. به صفحه "تسک‌ها" بروید

3. هر تسکی را جابجا کنید

### نتیجه مورد انتظار:
✅ همه تسک‌ها جابجا شوند (بدون محدودیت)

---

## تست 3: حذف تسک (30 ثانیه)

### فقط با ادمین:
1. Login کنید: Username: `admin` / Password: `admin123`

2. به صفحه "تسک‌ها" بروید

3. روی آیکون سطل زباله (🗑️) کلیک کنید

4. تایید حذف را بزنید

### نتیجه مورد انتظار:
✅ تسک حذف شود
✅ از لیست حذف شود

### با کاربر عادی:
- آیکون سطل زباله نمایش داده نمی‌شود ❌

---

## تست 4: یوزر استوری (1 دقیقه)

### اسپرینت:
1. با ادمین login کنید

2. به صفحه "اسپرینت‌ها" بروید

3. روی "اسپرینت جدید" کلیک کنید

4. فرم را پر کنید و در قسمت "یوزر استوری" بنویسید:
   ```
   به عنوان کاربر، می‌خواهم تسک‌هایم را مشاهده کنم تا بتوانم برنامه‌ریزی کنم
   ```

5. اسپرینت را ایجاد کنید

6. روی اسپرینت کلیک کنید تا جزئیات را ببینید

### نتیجه مورد انتظار:
✅ یوزر استوری در جزئیات نمایش داده شود (با پس‌زمینه آبی)

### بک‌لاگ:
همین کار را برای بک‌لاگ تکرار کنید

---

## تست 5: تقویم (30 ثانیه)

1. به صفحه "تقویم" بروید

### نتیجه مورد انتظار:
✅ تسک‌ها با رنگ‌های مختلف نمایش داده شوند
✅ جلسات با آیکون 👥 نمایش داده شوند
✅ اسپرینت‌ها با آیکون ✓ نمایش داده شوند
✅ تاریخ‌ها به شمسی باشند

---

## تست 6: تاریخ شمسی (30 ثانیه)

در تمام صفحات بررسی کنید:

### نتیجه مورد انتظار:
✅ همه تاریخ‌ها به فرمت شمسی باشند (مثلاً: 1404/11/21)
✅ در داشبورد
✅ در تسک‌ها
✅ در جلسات
✅ در اسپرینت‌ها
✅ در بک‌لاگ‌ها
✅ در تقویم
✅ در نوتیفیکیشن‌ها

---

## اگر همه تست‌ها موفق بود:

🎉 **تبریک! سیستم کامل و بدون باگ است!**

---

## اگر تستی ناموفق بود:

1. **Console Browser را باز کنید** (F12)
   - خطای دقیق را یادداشت کنید

2. **Terminal Backend را بررسی کنید**
   - لاگ‌های print شده را مشاهده کنید

3. **مراحل دیباگ:**
   ```bash
   # Backend را restart کنید
   Ctrl+C در terminal backend
   cd backend
   venv\Scripts\activate
   python manage.py runserver
   
   # Frontend را refresh کنید
   Ctrl+F5 در browser
   
   # دوباره login کنید
   ```

4. **اطلاعات مورد نیاز برای گزارش باگ:**
   - نام تست که ناموفق بود
   - پیام خطا از Console
   - لاگ‌های Terminal
   - نقش کاربری که با آن تست کردید

---

## اطلاعات کاربران:

| نام کاربری | رمز عبور | نقش |
|-----------|---------|-----|
| admin | admin123 | ادمین |
| scrum | scrum123 | اسکرام مستر |
| ali | ali123 | عضو تیم |
| reza | reza123 | عضو تیم |

---

## لینک‌های مفید:

- Backend API: http://127.0.0.1:8000/api/
- Frontend: http://localhost:5173/
- Admin Panel: http://127.0.0.1:8000/admin/

---

**زمان تخمینی کل تست‌ها: 5-7 دقیقه**
