# ✅ بروزرسانی دسترسی‌های جلسات

## تغییرات اعمال شده:

### 1. ✅ حذف جلسه توسط ادمین
**Backend:**
- تغییر `get_permissions` در `MeetingViewSet`
- فقط ادمین می‌تواند جلسه حذف کند (`destroy` action)

**Frontend:**
- اضافه کردن دکمه حذف (آیکون سطل زباله) به کارت جلسه
- فقط ادمین دکمه را می‌بیند
- تایید قبل از حذف

### 2. ✅ نمایش جلسات فقط برای شرکت‌کنندگان
**Backend:**
- اضافه کردن `get_queryset` به `MeetingViewSet`
- ادمین و اسکرام مستر: همه جلسات
- کاربران عادی: فقط جلساتی که شرکت‌کننده هستند

**تست شده:**
```
ادمین می‌بیند: 7 جلسه (همه)
علی می‌بیند: 2 جلسه (فقط جلسات خودش)
رضا می‌بیند: 3 جلسه (فقط جلسات خودش)
```

### 3. ✅ نوتیفیکیشن فقط برای شرکت‌کنندگان
**Backend:**
- نوتیفیکیشن فقط برای کسانی که در `attendees` هستند ارسال می‌شود
- کاربران دیگر نوتیفیکیشن دریافت نمی‌کنند

### 4. ✅ فیلتر تقویم
**Backend:**
- endpoint `/api/meetings/calendar/` از `get_queryset` استفاده می‌کند
- هر کاربر فقط جلسات خودش را در تقویم می‌بیند

---

## فایل‌های تغییر یافته:

### Backend:
1. **`backend/api/views.py`**
   - `MeetingViewSet.get_queryset()` - فیلتر جلسات
   - `MeetingViewSet.get_permissions()` - دسترسی حذف فقط برای ادمین
   - `MeetingViewSet.calendar()` - استفاده از get_queryset

### Frontend:
2. **`frontend/src/pages/Meetings.jsx`**
   - Import `Trash2` icon
   - `handleDeleteMeeting()` - تابع حذف جلسه
   - دکمه حذف در کارت جلسه (فقط برای ادمین)

---

## دسترسی‌ها:

### ادمین:
- ✅ مشاهده همه جلسات
- ✅ ایجاد جلسه
- ✅ ویرایش جلسه
- ✅ حذف جلسه
- ✅ دریافت نوتیفیکیشن (اگر شرکت‌کننده باشد)

### اسکرام مستر:
- ✅ مشاهده همه جلسات
- ✅ ایجاد جلسه
- ✅ ویرایش جلسه
- ❌ حذف جلسه
- ✅ دریافت نوتیفیکیشن (اگر شرکت‌کننده باشد)

### کاربر عادی:
- ✅ مشاهده فقط جلسات خودش
- ❌ ایجاد جلسه
- ❌ ویرایش جلسه
- ❌ حذف جلسه
- ✅ دریافت نوتیفیکیشن (فقط برای جلسات خودش)

---

## نکات مهم:

1. **نوتیفیکیشن:**
   - فقط به شرکت‌کنندگان ارسال می‌شود
   - در زمان ایجاد جلسه ارسال می‌شود

2. **تقویم:**
   - هر کاربر فقط جلسات خودش را می‌بیند
   - ادمین و اسکرام مستر همه جلسات را می‌بینند

3. **حذف:**
   - فقط ادمین می‌تواند جلسه حذف کند
   - تایید قبل از حذف

---

## تست:

### تست 1: نمایش جلسات
1. با کاربر عادی (ali) login کنید
2. به صفحه جلسات بروید
3. **انتظار:** فقط جلساتی که علی شرکت‌کننده است نمایش داده شود

### تست 2: حذف جلسه
1. با ادمین login کنید
2. به صفحه جلسات بروید
3. روی آیکون سطل زباله کلیک کنید
4. **انتظار:** جلسه حذف شود

### تست 3: نوتیفیکیشن
1. با ادمین جلسه جدید ایجاد کنید
2. فقط یک نفر را به عنوان شرکت‌کننده انتخاب کنید
3. با آن کاربر login کنید
4. **انتظار:** نوتیفیکیشن دریافت شود
5. با کاربر دیگری login کنید
6. **انتظار:** نوتیفیکیشن دریافت نشود

---

## وضعیت نهایی:

- ✅ حذف جلسه (ادمین)
- ✅ نمایش جلسات فقط برای شرکت‌کنندگان
- ✅ نوتیفیکیشن فقط برای شرکت‌کنندگان
- ✅ فیلتر تقویم
- ✅ ایجاد جلسه
- ✅ جابجایی تسک
- ✅ حذف تسک (ادمین)
- ✅ یوزر استوری
- ✅ تاریخ شمسی

🎉 **همه ویژگی‌ها کامل شدند!**
