# راهنمای مشارکت

از اینکه می‌خواهید در پروژه مشارکت کنید متشکریم! 🎉

## چگونه مشارکت کنیم؟

### گزارش باگ

اگر باگی پیدا کردید:

1. بررسی کنید که قبلاً گزارش نشده باشد
2. Issue جدید ایجاد کنید
3. توضیحات کامل بدهید:
   - مراحل بازتولید باگ
   - رفتار مورد انتظار
   - رفتار واقعی
   - اسکرین‌شات (در صورت امکان)
   - محیط (OS، Browser، نسخه)

### پیشنهاد ویژگی جدید

برای پیشنهاد ویژگی:

1. Issue جدید با برچسب "feature" ایجاد کنید
2. توضیح دهید چرا این ویژگی مفید است
3. مثال‌های کاربردی بزنید
4. منتظر بازخورد تیم باشید

### ارسال Pull Request

1. Fork کردن پروژه
2. ایجاد branch جدید:
   ```bash
   git checkout -b feature/amazing-feature
   ```
3. تغییرات خود را commit کنید:
   ```bash
   git commit -m 'Add some amazing feature'
   ```
4. Push به branch:
   ```bash
   git push origin feature/amazing-feature
   ```
5. Pull Request ایجاد کنید

## استانداردهای کد

### Python (Backend)

- از PEP 8 پیروی کنید
- از type hints استفاده کنید
- docstring برای توابع بنویسید
- تست بنویسید

```python
def create_task(title: str, assignee: User) -> Task:
    """
    ایجاد تسک جدید
    
    Args:
        title: عنوان تسک
        assignee: کاربر مسئول
        
    Returns:
        Task: تسک ایجاد شده
    """
    return Task.objects.create(title=title, assignee=assignee)
```

### JavaScript/React (Frontend)

- از ESLint پیروی کنید
- کامپوننت‌های functional بنویسید
- از hooks استفاده کنید
- PropTypes یا TypeScript استفاده کنید

```jsx
function TaskCard({ task }) {
  return (
    <div className="card">
      <h3>{task.title}</h3>
      <p>{task.description}</p>
    </div>
  )
}
```

## تست

قبل از ارسال PR:

```bash
# Backend
cd backend
python manage.py test

# Frontend
cd frontend
npm test
```

## Commit Messages

از Conventional Commits استفاده کنید:

- `feat:` ویژگی جدید
- `fix:` رفع باگ
- `docs:` تغییرات مستندات
- `style:` فرمت کد
- `refactor:` بازنویسی کد
- `test:` اضافه کردن تست
- `chore:` کارهای نگهداری

مثال:
```
feat: add task filtering by priority
fix: resolve deadline notification bug
docs: update API documentation
```

## ساختار Branch

- `main` - نسخه پایدار
- `develop` - نسخه توسعه
- `feature/*` - ویژگی‌های جدید
- `fix/*` - رفع باگ
- `hotfix/*` - رفع باگ فوری

## Code Review

تمام PR ها باید:

- تست شوند
- مستندات داشته باشند
- از استانداردها پیروی کنند
- توسط حداقل یک نفر review شوند

## سوالات؟

اگر سوالی دارید:

- Issue ایجاد کنید
- در Discussions شرکت کنید
- با تیم تماس بگیرید

## Code of Conduct

- محترمانه رفتار کنید
- از زبان مناسب استفاده کنید
- به نظرات دیگران احترام بگذارید
- سازنده باشید

## لایسنس

با مشارکت در این پروژه، موافقت می‌کنید که کد شما تحت لایسنس MIT منتشر شود.

---

متشکریم از مشارکت شما! 🙏
