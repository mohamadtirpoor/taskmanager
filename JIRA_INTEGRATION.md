# راهنمای اتصال به Jira

این راهنما مراحل کامل اتصال پلتفرم به Jira را شرح می‌دهد.

## 📋 پیش‌نیازها

1. یک حساب Jira (Cloud یا Server)
2. دسترسی ادمین به پروژه Jira
3. API Token از Jira

## 🔑 دریافت API Token از Jira

### مرحله 1: ایجاد API Token

1. به آدرس https://id.atlassian.com/manage-profile/security/api-tokens بروید
2. روی "Create API token" کلیک کنید
3. یک نام برای token انتخاب کنید (مثلاً "Platform Integration")
4. Token را کپی کنید و در جای امنی ذخیره کنید

### مرحله 2: پیدا کردن Project Key

1. به پروژه Jira خود بروید
2. Project Key را از URL یا تنظیمات پروژه پیدا کنید
3. معمولاً یک کد 2-5 حرفی است (مثل PROJ, DEV, TEAM)

## ⚙️ تنظیمات Backend

### 1. نصب کتابخانه‌های مورد نیاز

```bash
cd backend
pip install -r requirements.txt
```

### 2. تنظیم فایل .env

فایل `backend/.env` را ایجاد کنید و اطلاعات زیر را اضافه کنید:

```env
# Jira Integration
JIRA_URL=https://your-domain.atlassian.net
JIRA_EMAIL=mohamad.tirpoor@gmail.com
JIRA_API_TOKEN=ATATT3xFfGF0uE4jzOBFlAXsMURRxOYV2ezMCg_w3u5PUfrVNxBa7FGrXENivgNbFiBzGqfY8eON7WUOqWTgJRmZ7t13w4IENzETVVf2ZOPbjwbPkTSRfQuQnQzDXQihCtMlCwsJyha2MPHaMgvSzX4GX5hU39yO8mjdG3nKGWkjniHDYwtLD9U=6B630E1E
JIRA_PROJECT_KEY=YOUR_PROJECT_KEY
```

**نکته مهم:** `JIRA_PROJECT_KEY` را با کلید پروژه واقعی خود جایگزین کنید.

### 3. اجرای Migration

```bash
python manage.py migrate
```

### 4. تست اتصال

```bash
python manage.py jira_sync --test
```

اگر پیام موفقیت دریافت کردید، اتصال برقرار است! ✅

## 🔄 همگام‌سازی داده‌ها

### همگام‌سازی از طریق Command Line

#### انتقال همه داده‌ها به Jira
```bash
python manage.py jira_sync --direction to-jira
```

#### دریافت همه داده‌ها از Jira
```bash
python manage.py jira_sync --direction from-jira
```

#### همگام‌سازی دو طرفه
```bash
python manage.py jira_sync --direction both
```

#### دریافت تغییرات اخیر (24 ساعت گذشته)
```bash
python manage.py jira_sync --direction from-jira --recent 24
```

### همگام‌سازی از طریق رابط کاربری

1. به عنوان Admin وارد شوید
2. به صفحه "اتصال Jira" بروید
3. ابتدا "تست اتصال" را کلیک کنید
4. از دکمه‌های همگام‌سازی استفاده کنید:
   - **انتقال به Jira**: همه Tasks، Sprints و Backlogs را به Jira منتقل می‌کند
   - **دریافت از Jira**: همه Issues از Jira را دریافت می‌کند
   - **تغییرات اخیر**: فقط تغییرات 24 ساعت اخیر را دریافت می‌کند

## 🔔 تنظیم Webhook (همگام‌سازی خودکار)

برای همگام‌سازی خودکار تغییرات از Jira به پلتفرم:

### 1. تنظیم Webhook در Jira

1. به تنظیمات پروژه Jira بروید
2. به بخش "Automation" یا "Webhooks" بروید
3. یک Webhook جدید ایجاد کنید با مشخصات زیر:

**URL:**
```
https://your-domain.com/api/jira/webhook/
```

**Events:**
- Issue Created
- Issue Updated
- Issue Deleted
- Sprint Created
- Sprint Updated

**نکته:** اگر از localhost استفاده می‌کنید، باید از ابزارهایی مثل ngrok برای expose کردن سرور استفاده کنید.

### 2. استفاده از ngrok (برای توسعه محلی)

```bash
ngrok http 8000
```

سپس URL ارائه شده توسط ngrok را در Webhook جیرا استفاده کنید.

## 📊 نگاشت داده‌ها

### Task ↔ Jira Issue

| پلتفرم | Jira |
|--------|------|
| Task | Issue (Type: Task) |
| Title | Summary |
| Description | Description |
| Status (todo, in_progress, in_review, done) | Status |
| Priority (low, medium, high, urgent) | Priority |
| Assignee | Assignee |
| Deadline | Due Date |
| Tags | Labels |

### Sprint ↔ Jira Sprint

| پلتفرم | Jira |
|--------|------|
| Sprint | Sprint |
| Title | Name |
| Description | Goal |
| Start Date | Start Date |
| End Date | End Date |

### Backlog ↔ Jira Backlog

Backlog items در Jira به صورت Issues بدون Sprint ذخیره می‌شوند.

## 🎯 قابلیت‌های پشتیبانی شده

### ✅ پشتیبانی کامل

- ایجاد، ویرایش و حذف Tasks
- ایجاد و ویرایش Sprints
- اضافه کردن Tasks به Sprints
- همگام‌سازی Comments
- همگام‌سازی Tags/Labels
- همگام‌سازی Priority و Status
- همگام‌سازی Assignee

### 🔄 در حال توسعه

- همگام‌سازی Attachments
- همگام‌سازی Time Tracking
- همگام‌سازی Subtasks
- همگام‌سازی Epic Links

## 🔧 عیب‌یابی

### خطای اتصال

اگر پیام "خطا در اتصال" دریافت کردید:

1. بررسی کنید که `JIRA_URL` صحیح است
2. بررسی کنید که `JIRA_EMAIL` و `JIRA_API_TOKEN` صحیح هستند
3. بررسی کنید که دسترسی اینترنت دارید
4. بررسی کنید که API Token منقضی نشده است

### خطای Project Not Found

اگر پیام "پروژه یافت نشد" دریافت کردید:

1. بررسی کنید که `JIRA_PROJECT_KEY` صحیح است
2. بررسی کنید که به پروژه دسترسی دارید
3. بررسی کنید که پروژه فعال است

### خطای Permission Denied

اگر پیام "دسترسی رد شد" دریافت کردید:

1. بررسی کنید که کاربر Jira دسترسی لازم را دارد
2. بررسی کنید که API Token معتبر است
3. ممکن است نیاز به دسترسی Admin داشته باشید

## 📝 API Endpoints

### تست اتصال
```
GET /api/jira/test_connection/
```

### همگام‌سازی یک Task
```
POST /api/jira/sync_task_to_jira/
Body: { "task_id": 1 }
```

### همگام‌سازی یک Sprint
```
POST /api/jira/sync_sprint_to_jira/
Body: { "sprint_id": 1 }
```

### همگام‌سازی همه به Jira
```
POST /api/jira/sync_all_to_jira/
```

### همگام‌سازی همه از Jira
```
POST /api/jira/sync_all_from_jira/
```

### دریافت وضعیت همگام‌سازی
```
GET /api/jira/sync_status/
```

### Webhook Endpoint
```
POST /api/jira/webhook/
```

## 🎓 نکات مهم

1. **همگام‌سازی اولیه**: برای اولین بار، بهتر است از "انتقال به Jira" یا "دریافت از Jira" استفاده کنید، نه هر دو.

2. **Webhook**: برای همگام‌سازی مداوم، حتماً Webhook را تنظیم کنید.

3. **Conflict Resolution**: اگر یک آیتم در هر دو سیستم تغییر کرده باشد، آخرین تغییر اعمال می‌شود.

4. **Performance**: برای پروژه‌های بزرگ، از "تغییرات اخیر" به جای "همگام‌سازی کامل" استفاده کنید.

5. **Backup**: قبل از همگام‌سازی اولیه، از داده‌های خود backup بگیرید.

## 🆘 پشتیبانی

اگر مشکلی داشتید:

1. لاگ‌های Django را بررسی کنید
2. از دستور `python manage.py jira_sync --test` برای تست اتصال استفاده کنید
3. مستندات Jira API را مطالعه کنید: https://developer.atlassian.com/cloud/jira/platform/rest/v3/

## 🎉 موفق باشید!

حالا پلتفرم شما به طور کامل با Jira متصل است و می‌توانید از تمام قابلیت‌های Jira در کنار پلتفرم خود استفاده کنید.
