# راهنمای طراحی و تصاویر 🎨

## رنگ‌های استفاده شده:

### پالت رنگی اصلی:
- **Background:** `#F5F5F5` - پس‌زمینه اصلی
- **Card Light (سبز روشن):** `#CED986` - کارت‌های سبک
- **Card Purple (بنفش روشن):** `#ACA8CF` - کارت‌های بنفش
- **Dark Card:** `#2B2B2B` - کارت‌های تیره
- **Text Primary:** `#262626` - متن اصلی
- **Text Secondary:** `#868684` - متن ثانویه

---

## تصاویر مورد نیاز:

### 1. آواتارهای پیش‌فرض (اختیاری)
**مسیر:** `frontend/public/images/avatars/`

می‌توانید تصاویر زیر را اضافه کنید:
- `default-avatar.png` - آواتار پیش‌فرض (400x400 پیکسل)
- `avatar-1.png` تا `avatar-5.png` - آواتارهای نمونه

**نکته:** اگر تصویر نباشد، سیستم از حروف اول نام کاربر استفاده می‌کند.

### 2. لوگو (اختیاری)
**مسیر:** `frontend/public/images/`

- `logo.png` - لوگوی شرکت (200x60 پیکسل)
- `logo-dark.png` - لوگو برای پس‌زمینه تیره

---

## ویژگی‌های طراحی:

### کارت‌ها:
- **گوشه‌های گرد:** `rounded-3xl` (24px)
- **سایه:** `shadow-lg` با `hover:shadow-xl`
- **انیمیشن:** `transition-all duration-300`

### دکمه‌ها:
- **گوشه‌های گرد:** `rounded-2xl` (16px)
- **پدینگ:** `px-6 py-3`
- **سایه:** `shadow-md` با `hover:shadow-lg`

### آواتار:
- **شکل:** دایره کامل (`rounded-full`)
- **بوردر:** 2-4 پیکسل سفید
- **سایه:** `shadow-md` یا `shadow-lg`

### Progress Bar:
- **ارتفاع:** 12px (`h-3`)
- **گوشه‌های گرد:** `rounded-full`
- **گرادیانت:** از رنگ روشن به تیره

---

## صفحات جدید:

### 1. صفحه پروفایل (`/profile`)
**ویژگی‌ها:**
- نمایش اطلاعات کاربر
- آپلود عکس پروفایل
- ویرایش اطلاعات شخصی
- نمایش نقش کاربر با بج رنگی

**دسترسی:** همه کاربران

### 2. داشبورد بروزرسانی شده (`/`)
**ویژگی‌ها:**
- کارت‌های آماری با رنگ‌های مختلف
- نمایش پیشرفت شخصی
- نمایش پیشرفت تیم (برای ادمین/اسکرام مستر)
- تسک‌های عقب افتاده با کارت تیره
- لینک به پروفایل در هدر

---

## نحوه استفاده از تصاویر:

### 1. آواتار کاربر:
```jsx
// اگر تصویر دارد
<img src={user.avatar} alt="Avatar" />

// اگر تصویر ندارد (حروف اول نام)
<div className="bg-gradient-to-br from-primary-300 to-purple-300">
  {user.first_name[0]}{user.last_name[0]}
</div>
```

### 2. آپلود تصویر:
```jsx
<input 
  type="file" 
  accept="image/*" 
  onChange={handleAvatarChange}
/>
```

---

## کلاس‌های CSS سفارشی:

### کارت‌ها:
- `.card` - کارت سفید معمولی
- `.card-light` - کارت سبز روشن (#CED986)
- `.card-purple` - کارت بنفش روشن (#ACA8CF)
- `.card-dark` - کارت تیره (#2B2B2B)

### دکمه‌ها:
- `.btn-primary` - دکمه تیره
- `.btn-secondary` - دکمه خاکستری
- `.btn-light` - دکمه سبز روشن
- `.btn-purple` - دکمه بنفش روشن

### بج‌ها:
- `.badge-primary` - بج سبز
- `.badge-purple` - بج بنفش
- `.badge-dark` - بج تیره

### آواتار:
- `.avatar` - آواتار کوچک (40x40)
- `.avatar-lg` - آواتار بزرگ (80x80)

---

## مثال‌های کد:

### کارت آماری:
```jsx
<div className="stat-card bg-gradient-to-br from-primary-200 to-primary-300">
  <div className="flex items-center justify-between mb-4">
    <div className="bg-white bg-opacity-50 rounded-2xl p-3">
      <CheckCircle2 size={28} />
    </div>
    <ArrowUpRight size={20} />
  </div>
  <p className="text-text-secondary text-sm mb-1">کل تسک‌ها</p>
  <p className="text-4xl font-bold text-text-primary">24</p>
</div>
```

### Progress Bar:
```jsx
<div className="progress-bar">
  <div 
    className="progress-fill bg-gradient-to-r from-primary-400 to-primary-600"
    style={{ width: '75%' }}
  />
</div>
```

### آواتار با حروف:
```jsx
<div className="w-12 h-12 rounded-full bg-gradient-to-br from-primary-300 to-purple-300 flex items-center justify-center text-xl font-bold text-white">
  {user.first_name[0]}{user.last_name[0]}
</div>
```

---

## نکات مهم:

1. **تصاویر اختیاری هستند** - سیستم بدون تصویر هم کار می‌کند
2. **حروف اول نام** به عنوان آواتار پیش‌فرض استفاده می‌شود
3. **رنگ‌ها** از Tailwind config می‌آیند
4. **انیمیشن‌ها** smooth و سریع هستند (300ms)
5. **همه المان‌ها** responsive هستند

---

## تست طراحی:

### چک‌لیست:
- [ ] رنگ‌ها مطابق پالت هستند
- [ ] گوشه‌ها گرد هستند (rounded-3xl)
- [ ] سایه‌ها نرم و زیبا هستند
- [ ] انیمیشن‌ها smooth هستند
- [ ] آواتارها دایره کامل هستند
- [ ] Progress bar ها گرادیانت دارند
- [ ] کارت‌ها hover effect دارند
- [ ] فونت Vazirmatn است
- [ ] متن‌ها راست‌چین هستند

---

## پوشه‌های تصاویر:

```
frontend/public/images/
├── avatars/          # آواتارهای کاربران
│   ├── default-avatar.png
│   ├── avatar-1.png
│   └── ...
├── logo.png          # لوگوی شرکت
└── logo-dark.png     # لوگو برای پس‌زمینه تیره
```

**همه تصاویر اختیاری هستند و سیستم بدون آنها هم کار می‌کند!**
